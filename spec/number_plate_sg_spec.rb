require "spec_helper"

describe NumberPlateSg do
  it "has a version number" do
    expect(NumberPlateSg::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
